# VibeVoice API Server Configuration

# Models directory (where VibeVoice models are stored)
# In production, this is shared with ComfyUI models directory
MODELS_DIR=/workspace/ComfyUI/models/vibevoice

# Server configuration
HOST=0.0.0.0
PORT=8100

# Default model to load on first request
# Options: VibeVoice-1.5B, VibeVoice-Large, VibeVoice-Large-Q8, VibeVoice-Large-Q4
DEFAULT_MODEL=VibeVoice-1.5B

# Enable CORS for cross-origin requests
ENABLE_CORS=true

# Cache directories (shared across services to prevent re-downloads)
HF_HOME=/data/.huggingface
TORCH_HOME=/data/.torch
TRANSFORMERS_CACHE=/data/.huggingface/transformers

# GPU configuration (set to specific GPU index or leave empty for auto)
CUDA_VISIBLE_DEVICES=0
NVIDIA_VISIBLE_DEVICES=0

# Performance optimizations for RTX 5090
PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True
CUDA_MODULE_LOADING=LAZY
